# Bitget build isolation

Temporary note: Vercel connector logs were unavailable during the v3 iteration. Build isolation is performed via disposable debug branches so the feature branch remains intact. Remove this note after the preview is green.
